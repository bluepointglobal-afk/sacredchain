# Base URL of the Sacred Knowledge API (backend). The Socket.io client derives
# its origin from this by stripping the trailing /api.
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Jitsi video domain (use meet.jit.si for quick start, or your self-hosted domain).
NEXT_PUBLIC_JITSI_DOMAIN=meet.jit.si
